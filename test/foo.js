const expect = require('chai').expect;

const fun = require('../foo.js').fun;

describe('foo tests', () => {
  it('should return aKicsi', function () {
    const result = fun(2, 10);
    expect()
  });
});
